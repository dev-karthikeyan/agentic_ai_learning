from langchain.tools import tool

@tool
def get_weather (city : str) str :
    """ you are a weather tool """
    return f"weather is hot in {city}"

model_with_tools = model.bind_tools([get_weather])

date= model_with_tools()